<section class="order_form">
  <h1 id="order_heading">New Order</h1>

  <%= render 'form', order: @order %>

  <%= link_to ' Go Back To The Store', store_index_path, :class => "back_tag" %>
  <%= javascript_pack_tag("pay_type") %>
</section>
